const express = require("express");
const routes = express.Router();

module.exports = (db) => {
  routes.post("/signup", (req, res) => {});
  routes.post("/signup", (req, res) => {});
  return routes;
};
